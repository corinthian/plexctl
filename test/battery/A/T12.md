## Commands

```
$ plexctl context --history-limit 5
{"error":"missing config key: default_client — run plexctl auth login","ok":false}
exit code: 1
```

```
$ plexctl now-playing
{"error":"missing config key: default_client — run plexctl auth login","ok":false}
exit code: 1
```

## Reply

plexctl not set up. Run `plexctl auth login`.
