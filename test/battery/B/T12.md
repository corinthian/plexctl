## Commands

```
$ plexctl now-playing
{"error":{"code":"PLEX_AUTH_REQUIRED","hint":"run: plexctl auth login","message":"missing config key: default_client — run plexctl auth login"},"ok":false}
EXIT:5
```

## Reply

plexctl isn't logged in. Run `plexctl auth login`.
