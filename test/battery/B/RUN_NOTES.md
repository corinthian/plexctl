# Battery B-Run Notes (2026-07-20)

Pairing: **v2 binary** (`plexctl 2.0.0`, run via the shims' passthrough to `dist/plexctl` — the ~/.local/bin install was pending at run time) + **v2 skill** (the rewritten SKILL.md + REFERENCE.md, live-swapped per the release checklist; v1 preserved as SKILL.v1.md.bak). Agent: Sonnet, one fresh agent per task, identical utterances and mechanism to the A run.

Deltas from A/RUN_NOTES.md, everything else identical:

- Shims regenerated by `build_battery_b.py` (committed alongside): same titles/ratingKeys/interception points; every injected/canned ERROR payload swapped to its v2 envelope (`PLEX_QUEUE_STAGED`/`PLEX_QUEUE_CONFLICT` exit 2 with hints and `data`, `PLEX_CLIENT_UNREACHABLE` exit 3, `PLEX_SMART_CONTAINER`, `PLEX_SCOPE_REQUIRED`, BAD_REQUEST for wrong-key play-media). Success shapes unchanged (v2 kept flat success envelopes).
- T07's bare `play` now serves the real v2 behavior: `NOT_APPLIED` exit 6 with the play-media hint (A served v1's silent `ok:true`).
- T16's guard serves `PLEX_UNSUPPORTED` exit 2 (v2 binary owns the shuffle refusal; under the v2 skill, invoking the binary here is the designed flow, not a violation).
- T12 runs the real v2 binary against an empty config dir → genuine `PLEX_AUTH_REQUIRED`, exit 5.
- T01/T10 live against the real server through the v2 binary (T10 exercises real `durationNominal`).
- Agent prompts identical to A except the forbid-list names SKILL.v1.md.bak instead of SKILL.v2.md (REFERENCE.md is part of the live v2 skill and is permitted).
- T02 note for future reruns: the B agent's library-list excursion passed through to the REAL library (reads only). Harmless, recorded in the transcript verbatim.
- Post-run: user queue verified intact (ID 5772, same 3 titles); v1 binary restored to ~/.local/bin pending the user's manual v2 install.

Scoring for both runs: `../SCORING.md`.
