# Build & test artifacts
/dist/
*.test
*.out
/plexctl

# Local config & secrets — config.toml holds the Plex token
config.toml
/.config/

# macOS
.DS_Store
._*

# Editor
*.swp

# Installed-skill local files (never in the repo)
.claude/skills/plex/PERSONAL.md
.claude/skills/plex/INCIDENTS.md
